TheBridgeGame has been moved to a standalone directory.

Open and build from:  d:\TheBridgeGame\TheBridgeGame.sln

This folder (inside the engine repo) can be deleted manually after closing any programs that might have it open (e.g. Visual Studio, Explorer).
